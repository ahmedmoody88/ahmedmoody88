# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this repository is

This repo is named `ahmedmoody88/ahmedmoody88` — identical to the owner's GitHub username. GitHub treats this as a special "profile repository": the `README.md` at its root is rendered directly on the owner's public GitHub profile page (github.com/ahmedmoody88).

There is no application code, build system, package manifest, or test suite in this repository, and none should be assumed. The only meaningful content is `README.md`.

## Working in this repository

- The primary (and typically only) file to edit is `README.md`. Treat changes to it as edits to a public-facing personal profile page, not as application source code.
- There are no build, lint, or test commands — none apply because there is no code to compile, lint, or test. Do not invent tooling (package.json, CI configs, etc.) unless the user explicitly asks for a specific feature (e.g., GitHub Actions for auto-updating stats).
- Keep `README.md` content genuinely representative of the owner; do not fabricate biographical details, skills, project lists, or statistics that haven't been provided.
- GitHub Flavored Markdown renders as-is on the profile page, so formatting (badges, images, alignment) should be verified against how GitHub actually renders profile READMEs.
